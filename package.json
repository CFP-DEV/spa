{
  "name": "spa-server",
  "version": "1.0.0",
  "description": "Fake REST API",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 5000",
    "client": "cd client && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "*"
  }
}
